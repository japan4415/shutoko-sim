[package]
name = "shutoko-graph-builder"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
shutoko-routing-core = { path = "../routing-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
