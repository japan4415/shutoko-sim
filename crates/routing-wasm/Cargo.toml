[package]
name = "shutoko-routing-wasm"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
shutoko-routing-core = { path = "../routing-core" }
wasm-bindgen = "=0.2.128"
